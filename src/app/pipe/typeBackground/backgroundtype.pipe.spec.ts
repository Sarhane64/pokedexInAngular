import { BackgroundtypePipe } from './backgroundtype.pipe';

describe('BackgroundtypePipe', () => {
  it('create an instance', () => {
    const pipe = new BackgroundtypePipe();
    expect(pipe).toBeTruthy();
  });
});
